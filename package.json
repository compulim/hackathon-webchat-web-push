{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "concurrently --kill-others npm:start:*",
    "start:app": "cd packages && cd app && npm start",
    "start:server": "cd packages && cd server && npm start"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "concurrently": "5.3.0"
  }
}
